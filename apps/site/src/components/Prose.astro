---
import { render } from "astro:content";
import { pageEntry } from "../lib/pages";

interface Props {
  id: string;
}
const entry = await pageEntry(Astro.props.id);
const { Content } = await render(entry);
---

<div class="prose">
  <Content />
</div>
