[tools]
# Node 24 is the active LTS line. Node 26 becomes LTS in October 2026; move then.
node = "24.21.0"
pnpm = "12.4.2"
just = "1.58.0"
# Security scanners, run by `just security` locally and in CI.
gitleaks = "8.30.1"
zizmor = "1.30.1"
osv-scanner = "2.6.0"
actionlint = "1.7.12"
